<h1>Selamat Datang</h1>
<h2>Hallo <%= nama %>
</h2>
<h4>Daftar Mahasiswa</h4>
<% if (mhs.length < 1) { %>
    <p>Data Mahasiswa masih kosong</p>
    <% }else{%>

        <% mhs.forEach(mhs=> { %>

            <ul>
                <li>
                    nama: <%= mhs.nama %>
                </li>
                <li>
                    email: <%= mhs.email %>
                </li>
            </ul>
            <% }) %>
                <% } %>